import 'package:flutter/material.dart';

class AppColors {
  // eResto Aligned Theme Colors
  static const Color primary = Color(0xFFAB2421);       // Crimson Red (host seed)
  static const Color primaryDark = Color(0xFF7A0000);   // Dark Crimson
  static const Color primaryLight = Color(0xFFFAE6E5);  // Selection Light Red Tint

  // Statuses
  static const Color success = Color(0xFF1E6B3C);
  static const Color successLight = Color(0xFFEAF7EF);
  static const Color warning = Color(0xFF8A5200);
  static const Color warningLight = Color(0xFFFFF5E6);
  static const Color error = Color(0xFFDC2626);

  // Veg / Non-Veg Indicators
  static const Color veg = Color(0xFF1E6B3C);
  static const Color nonVeg = Color(0xFFAB2421);

  // Neutrals
  static const Color text = Color(0xFF0F0F0F);          // Ink900
  static const Color textDark = Color(0xFF111827);      // Charcoal Dark
  static const Color textSecondary = Color(0xFF4B4B4B); // Ink600
  static const Color textMuted = Color(0xFF9A9A9A);     // Ink400
  static const Color border = Color(0xFFE4E4E2);        // Ink200
  static const Color borderLight = Color(0xFFF0F0F0);
  static const Color background = Color(0xFFF5F5F5);    // Scaffold Background
  static const Color surface = Color(0xFFFFFFFF);       // White card/surface
}
